func factorial(value)
    if value <= 1 then
        return 1
    end
    return value * factorial(value - 1)
end

var message = "Hello "

message += "World!"

for var i = 0, i < strlen(message), i += 1 do
    print(message[i])
    if i >= 9 then
        break
    end
end
