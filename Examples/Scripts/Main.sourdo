func set_apple_count(new_apple_count)
    if new_apple_count < 0 then
        apple_count = 0
    else
        apple_count = new_apple_count
    end
end

func factorial(value)
    if value <= 1 then
        return 1
    end
    return value * factorial(value - 1)
end

set_apple_count(factorial(4))
